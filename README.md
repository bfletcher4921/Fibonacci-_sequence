# Fibonacci-_sequence
Calculates fibonacci sequence
Prompts for user input for the length of the desired fib series.
Uses if loop to determine if input is invalid.
Creates list for the fib sequence.
Uses a while loop to add to the sequence then displays the completed sequence.
Our class used this project to develope familiarity with Loops using len, lists, and calculations.
